# Задание агенту — i18n-1 · Шаг 6 из 12: Каталог и страницы категорий

**Тип задачи:** вынос строк в словарь
**Зависимости:** i18n-1 · Шаг 1
**Связанные пункты плана:** `plan-3-i18n-constants.md` §1.3
**Файлы зоны:** `app/(public)/catalog/page.tsx`, `catalog/[slug]/page.tsx`, `components/category/CategorySort.tsx`, `CategoryCard.tsx`, `components/ui/EmptyState.tsx`

## Цель

UI-строки каталога — в словарь (namespace `catalog`). SEO-строки (catalog/page.tsx 14–21, [slug] 23) — шаг 11. Имена категорий из БД — i18n-2.

## Шаги

1. Ключи `catalog.*`:
   - catalog/page.tsx: крошки «Главная / Каталог», «✹ Витрина и полки ✹», «Каталог», «всё в одном экземпляре — …», «// много разделов …» (48–61); блок «Не нашли своё?» + CTA конфигуратора (83–89);
   - **plural-по-slug (41–43)** — «комплект», «услуга · от {price} ₴», «изделие»: привязка к slug остаётся, формы и шаблоны — в словаре (ключи вида `catalog.unit.{slug}` + фолбэк «изделие»); пометить хрупкость в отчёте;
   - [slug]/page.tsx: фильтры наличия «Любое наличие / В наличии / Под заказ / Только новинки» (48–51), чипы «Все», «Цена до {n}», «От {n}» (81–92), крошки (157–158), «✹ полка №{n} ✹» (164), «Фильтры и сортировка», «найдено: {n}» (174–176);
   - CategorySort: «Сортировка: сначала новинки», «Сначала дешевле», «Сначала дороже», aria-label (6–8, 26);
   - CategoryCard: title «Нет шаблона слотов» (33);
   - EmptyState: заголовок, текст, кнопка «Собрать своё» (9–15).
2. Заменить хардкод на `t()`; «найдено: {n}» — интерполяция.

## Критерии приёмки

1. RU: каталог и страницы категорий текстово идентичны (фильтры, чипы, сортировка, пустые состояния, спец-категории `komplekty`/`vintazhnyj-remaint` — проверить оба slug-случая).
2. EN/UK переключаются, plural «найдено» — по правилам языка.
3. Grep зоны — 0 кириллицы вне комментариев; `check:i18n`; `npm run lint`, `npm run build`.

## Не делать (out of scope)

- SEO-строки (шаг 11), данные категорий (i18n-2), логика фильтров.
