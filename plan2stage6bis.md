# Задание агента — Этап 6-бис. Завершение Этапа 6 (точечные доработки, замеры, отчёт и фиксация)

**Тип задачи:** верификация / замеры / документирование + малые точечные доработки
**Зависимости:** основная часть Этапа 6 закоммичена (коммит `fec2200` «Реализация Этапа 6») + 17 полировочных коммитов после него (`fbf94eb` … `cb2153b` — заявки, Telegram, категории, фото товара). Этапы 1–5 приёмлены, чекбоксы T-1.1…T-5.4 в `plan-2.md` §8 отмечены.
**Связанные пункты плана:** `plan-2.md` §0 (золотые правила), §8 Этап 6 (T-6.1 … T-6.3 — пока НЕ отмечены); `plan2stage6.md` целиком (решения руководителя от 2026-08-26 №1–№6 — действуют без изменений).
**Версия плана:** plan-2.md (итерация 2) + plan-finances2.md — единственные действующие.

---

## Цель

Довести Этап 6 до приёмки: выполнить оставшиеся пункты `plan2stage6.md` (замеры и фиксации, которые нельзя было сделать до стабилизации кода), зафиксировать результаты измерений в отчёте руководителю и — после подтверждения приёмки — отметить чекбоксы T-6.1 … T-6.3 в `plan-2.md` §8 с записью в «Журнал изменений».

Критерий успеха этапа: отчёт руководителю по `plan2stage6.md` сдан (замеры Lighthouse, протокол адаптив-сверки, размеры бандлов, ответы на вопросы), все команды проверки проходят, отличий «сделано, но не зафиксировано» не осталось.

---

## Золотые правила (§0 плана) — обязательны

1. `mockup/` — источник истины. Вёрстку/тексты/классы не переделывать.
2. Никакого хардкода редактируемых данных (решение от 2026-08-25).
3. Цены — только через `formatPrice`/`Priced` из `lib/format.ts`.
4. Файлы `mockup/**` не редактировать. **CSS/JS скинов не трогать** — уже сделанное отступление (`.tagline`, см. Контекст) не расширять; новые потребности в правках CSS — вопрос руководителю, не правка.
5. Коммиты — только по явной команде руководителя.

---

## Контекст — что уже сделано в Этапе 6 (НЕ переделывать)

| Пункт plan2stage6.md | Статус | Свидетельство |
|---|---|---|
| Шаг 0: фото Unsplash → `public/uploads/products/`, локальные пути в БД | сделано | 21+ jpg в `site/public/uploads/products/` (имена по slug); `seed.ts` пишет `/uploads/products/<file>` |
| Шаг 0: `imgWith()` удалён; ссылок на `images.unsplash.com` нет | сделано | grep по `site/` (ts/tsx/js/json) пуст |
| Шаг 0: seed/check-seed обновлены | сделано | `check-seed.ts` проверяет наличие файлов |
| Шаг 1: `error.tsx` public + admin | сделано | `app/(public)/error.tsx`, `app/admin/(panel)/error.tsx` |
| Шаг 1: `loading.tsx` | сделано | `/catalog`, `/catalog/[slug]`, `/configurator/[slug]`, `/admin/orders`, `/admin/products` |
| Шаг 1: `EmptyState` на пустых списках | частично | есть на главной, каталоге, категории; **нет на `/configurator`** (см. Шаг 1) |
| Шаг 2: `SITE_URL` + `metadataBase` | сделано | `lib/site-url.ts`, `.env.example`, `app/layout.tsx` |
| Шаг 2: `generateMetadata` товара с `metaTitle/metaDescription/ogImage` | сделано | `app/(public)/product/[slug]/page.tsx:15-26` |
| Шаг 2: description + OG на публичных страницах | сделано | catalog, configurator, about, contacts, main |
| Шаг 2: noindex служебных | сделано | `order-success` — `robots:{index:false}`; `robots.ts` disallow `/admin`, `/api`, `/order-success`; 404 отдаёт статус 404. Проверить noindex-мету на `not-found.tsx` (Шаг 1) |
| Шаг 2: `sitemap.ts`, `robots.ts` | сделано | 41 + 13 строк |
| Шаг 2б: Google Maps embed на `/contacts` | сделано | `contacts/page.tsx:58`, адрес из Settings |
| Шаг 3: `next/image` в витринных местах | сделано | `ProductCard`, `ProductGallery`; модалки/canvas/админка — `<img>` (по плану) |
| Шаг 3: sharp | **не требуется** | Next 16.3.1 сам тянет `sharp@^0.35.3` как свою зависимость (`node_modules/sharp` присутствует); добавить в `package.json` нужно только если prod-ран rugается (проверить в Шаге 3) |
| Шаг 3: konva только в чанках конфигуратора | сделано (`ssr:false`) | проверить размеры в Шаге 3, цифры — в отчёт |

**Существующее отступление от «CSS не трогать» (закоммичено в `fec2200`, руководитель не спрашивался):** в оба скина добавлен класс `.signboard .tagline` (+8 строк в `style-memphis.css` и `style.css`) — широкий переносимый блок подписи вместо чипа `.tag` (длинные подписи вылезали из-за nowrap). Используется на `/configurator` и, возможно, других signboard. **Не откатывать** (поздние коммиты на него опираются), но включить в отчёт отдельной строкой для ратификации.

**Незакоммиченное:** `.gitignore` (+`sample-design/images/`, +`site/*.zip`) — закоммитить по команде руководителя вместе с изменениями этого этапа или отдельно.

---

## Порядок работы

Последовательно: Шаг 0 → 1 → 2 → 3 → 4 → 5. Шаги 0–1 — код; Шаги 2–4 — только замеры/протоколы (код не менять, кроме блокеров, согласованных отдельно); Шаг 5 — отчёт и фиксация.

### Шаг 0. Инвентаризация расхождений с критериями приёмки

Пройтись по «Критериям приёмки» `plan2stage6.md` (пп. 1–8) сверху вниз и составить чек-лист «выполнено / расхождение». Исправлять сразу только то, что входит в Шаг 1; остальное — в отчёт.

### Шаг 1. Точечные доработки (единственные правки кода в этом этапе)

1. **`EmptyState` на `/configurator`** (`app/(public)/configurator/page.tsx`): если `withTemplate` пуст (нет категорий с `hasSlotTemplate=true`), вместо пустой `div.shelf` рендерить существующий `components/ui/EmptyState.tsx` (копия макета, кнопка «Собрать своё» → `/configurator` там неуместна — текст внутри `EmptyState` уже подходит, компонент не менять; при пустом списке секцию «С чего начнём?» оставлять). Малое изменение `if (withTemplate.length === 0)` → рендер `EmptyState`.
2. **noindex на 404**: проверить, отдаёт ли `app/not-found.tsx` мету `robots:{index:false}` (для страницы 404 статус-код уже неиндексируемый — мета нужна по букве плана, п. Шаг 2 «/order-success, /404: index:false»). Если нет — добавить `export const metadata` в `not-found.tsx`.
3. **Favicon**: НЕ рисовать и НЕ подключать свой (в макете иконки нет). Текущий `site/app/favicon.ico` — дефолт скаффолда create-next-app (25 КБ). Зафиксировать вопрос руководителю №1 (см. ниже), файл не трогать до решения.

### Шаг 2. Адаптив-сверка + протокол

1. `npm run dev`; для каждой публичной страницы (`/`, `/catalog`, `/catalog/kulony`, `/product/kulon-telegramma`, `/configurator`, `/configurator/kulony`, `/about`, `/contacts`, `/order-success/N`, `/404`) сверить поведение при сужении окна против её файла макета:
   - 768–1079px: бургер вместо nav, 2 колонки полок;
   - ≤820px: 1 колонка, калькулятор конфигуратора фиксируется снизу, работают `conf-tabs` (Коллаж / Слоты и цена).
2. Сверять поведение (что скрывается/перестраивается), не пиксели. Результат по каждой странице — строка в протоколе: `страница · брейкпоинт · ок / расхождение (описание)`.
3. Расхождение, требующее правки CSS скинов, — НЕ править: записать в отчёт вопросом руководителю (правило §0). Всё, что чинится разметкой/классами из макета, — чинить сразу, протоколировать как исправленное.
4. Отдельно сверить админку на ≤820px (таблицы скроллятся, бургер работает) — протокол, без правок CSS.

### Шаг 3. Prod-проверка сборки

1. `npm run build` — без ошибок и предупреждений нового кода; **выписать размеры чанков** из вывода, убедиться, что `konva`/`react-konva` находятся только в чанках роута `/configurator/[slug]` (не в общих). Цифры — в отчёт.
2. `npm run start` → убедиться, что изображения оптимизируются (запросы вида `/_next/image?url=...` отдают 200, нет ошибок про отсутствие sharp). Если sharp ругается — добавить в dependencies, отметить в отчёте; иначе не добавлять.
3. На prod-сервере (localhost:3000) прогнать быстрый smoke: главная, каталог, товар, конфигуратор-категория, contacts (карта грузится), переключение скина и валюты (один stylesheet на странице, цены пересчитываются), админка логинится.
4. Остановить сервер после замеров.

### Шаг 4. Lighthouse ≥ 90 (решение руководителя №3)

1. Условия: prod-сборка (`npm run build && npm run start`), Chrome, режим **Mobile**, страницы: `/`, `/catalog`, `/product/<любой>`, `/configurator/<любой>`, `/contacts`.
2. Инструмент: Chrome DevTools → Lighthouse вручную, либо CLI: `npx lighthouse http://localhost:3000 --form-factor=mobile --screenEmulation.mobile --chrome-flags="--headless=new" --output=json --output-path=...`. Каждая страница — отдельный прогон; между прогонами страницу кэшировать не запрещено, но все прогоны — в одинаковых условиях (указать в отчёте какие).
3. Цели: **Performance ≥ 90** на каждой странице; SEO/Best Practices/Accessibility — без критических провалов (< 80 фиксируется отдельно с пояснением).
4. Если < 90 — чинить найденное (явные width/height у изображений, `sizes` у `next/image`, `priority` первой четвёрки витрины, шрифты, лишний JS) и **повторять замер**. Все прогоны (до/после) — в отчёт.
5. Если после 2–3 итераций фиксируемых причин не осталось, а балл < 90 (внешние шрифты, headless-специфика) — остановиться и записать вопрос руководителю с последними цифрами и разбором вкладов метрик (LCP/CLS/TBT). Не «оптимизировать» бесконечно.

### Шаг 5. Финальная верификация и отчёт

1. Прогоны: `npm run build`, `npm run lint`, `npm run db:seed`, `npm run db:check` — все без ошибок. `db:seed` идемпотентен (двойной запуск — тот же результат).
2. Grep-контроль: `images.unsplash.com` в `site/` не находится; `mockup/**` не изменялись с прошлого коммита (`git status` чист по ним).
3. **Отчёт руководителю** (по форме раздела «Порядок и отчёт» `plan2stage6.md`):
   - что сделано в 6-бис (список изменений Шага 1);
   - команды проверки и их результат;
   - протокол адаптив-сверки (таблица постранично);
   - размеры бандлов + подтверждение «konva только в конфигураторе»;
   - цифры Lighthouse: все прогоны по 5 страницам (Performance/SEO/BP/A11y), до/после исправлений, условия замера;
   - список расхождений адаптива, требующих правки CSS (если найдены) — вопросами;
   - вопрос №1 (favicon), ратификация CSS-отступления `.tagline`, судьба незакоммиченного `.gitignore`;
   - готовая запись для «Журнала изменений» plan-2.md (дата; «Этап 6 завершён: локализация изображений, состояния, SEO (metadataBase/OG/sitemap/robots), Google Maps, next/image; замеры Lighthouse и адаптив-сверка зафиксированы; правки CSS скинов: класс .signboard .tagline»).
4. После подтверждения приёмки руководителем:
   - отметить чекбоксы **T-6.1, T-6.2, T-6.3** в `plan-2.md` §8;
   - добавить запись в «Журнал изменений» `plan-2.md`;
   - коммит — только по явной команде (в неё входят: правки Шага 1, `.gitignore`, обновления plan-2.md и этот файл).

---

## Критерии приёмки (Этапа 6-бис)

1. Выполнены все ранее не закрытые пункты `plan2stage6.md`: EmptyState на `/configurator` при пустом списке категорий с шаблонами; noindex на 404.
2. Протокол адаптив-сверки сдан: все 10 публичных страниц × 2 брейкпоинта; расхождения, требующие правки CSS, зафиксированы вопросами (скины не правились).
3. Замеры Lighthouse сданы: 5 страниц, mobile, prod-сборка, Performance ≥ 90 на каждой (или вопрос руководителю с разбором причин после 2–3 итераций исправлений).
4. Размеры бандлов зафиксированы; konva/react-konva подтверждены только в чанках `/configurator/[slug]`.
5. `npm run build` / `lint` / `db:seed` / `db:check` — без ошибок; переключатель скина/валюты работает; `mockup/**` не изменены.
6. Отчёт по Этапу 6 сдан; T-6.1…T-6.3 отмечены в `plan-2.md` §8; запись в «Журнале изменений» добавлена.

## Не делать (out of scope)

- Новые фичи, изменение логики заявок/конфигуратора/админки, правки schema БД.
- Правки CSS скинов (включая расширение `.tagline`) и любые файлы `mockup/**`.
- Подключение собственного favicon/иконки (вопрос №1), настройки `remotePatterns`, деплой (Этап 7).
- Коммиты без команды руководителя.

## Вопросы руководителю (включить в отчёт Шага 5)

1. **Favicon**: в макете иконки нет; в `site/app/favicon.ico` лежит дефолт скаффолда Next.js. Варианты: (а) оставить дефолт до появления бренд-иконки; (б) руководитель предоставляет иконку — подключим как `app/icon.*`; (в) временная SVG-заглушка в стиле комплектующих (нарушает «не выдумывать» — только по явному решению). По золотым правилам сами ничего не рисуем.
2. **Ратификация CSS-отступления**: класс `.signboard .tagline` добавлен в оба скина коммитом `fec2200` без отдельного согласования (устраняет вылезание длинных подписей из-за `nowrap` у `.tag`). Подтвердить осознанное исключение из правила «CSS не трогать» или дать иное решение.
3. **`.gitignore`**: незакоммиченные строки `sample-design/images/`, `site/*.zip` — закоммитить в составе этапа?
4. Расхождения адаптива, требующие правки CSS (если найдутся в Шаге 2), — по каждому отдельным вопросом.

## Порядок и отчёт

Последовательно: Шаг 0 → 1 → 2 → 3 → 4 → 5. Это замыкающий этап перед Этапом 7 (деплой); все измерения делаются на стабилизированном коде (последний коммит `cb2153b`). Итог — отчёт руководителю по Шагу 5 и фиксация T-6.1…T-6.3 в `plan-2.md` §8 после приёмки.
