# Ревью этапов 1–3 (plan-2.md) — состояние на 2026-08-15

> Проверено: дерево репо (коммиты 69b2135 «Этап 1», 2ed9bc6 «Этап 2», 57c789c «Этап 3»), все файлы `site/`, хэши копий CSS/JS, `npm run build`, `npm run lint`, `npm run db:seed`, `npm run db:check`.
> Общий вывод: **этапы 1–3 выполнены**, данные совпадают с макетом по D-11, сборка и lint чистые. Остались 2 процессных хвоста (чекбоксы и журнал изменений — см. §4).

---

## 1. Этап 1 «Фундамент» (T-1.1, T-1.2, T-1.3) — ВЫПОЛНЕН

### Что сделано
- **T-1.1** Next.js 16.3.1 + TypeScript (App Router, без Tailwind, без `src/`, алиас `@/*`). Tailwind/UI-фреймворков в `package.json` нет.
- Копии скинов **байт-в-байт** (SHA-256 совпадают с макетом):
  - `mockup/assets/css/style-memphis.css` = `site/public/css/style-memphis.css`
  - `mockup/assets/css/style.css` = `site/public/css/style.css`
  - `mockup/assets/js/skin-switcher.js` = `site/public/js/skin-switcher.js`
- `app/layout.tsx`: `<html lang="ru">`, **единственный** `link[rel=stylesheet]` на `/css/style-memphis.css`, `skin-switcher.js` подключён; в коде нет импортов CSS и `next/font`.
- **T-1.2** `Header`/`MobileMenu`/`Footer` (копии topbar/mobile-menu/footer из home.html), `nav-links.ts`, дефолты Settings в `lib/settings.ts` (телефон/email/адрес/часы строго из макета), вывеска-герой на `/` в `app/(public)/page.tsx`.
- **T-1.3** Drizzle + better-sqlite3: `drizzle/schema.ts` по §5 (7 таблиц, `productComponents` не создана, поля `availability/reserveUntil/orderDays` есть, `inStock` нет), миграция `0000_amused_bishop.sql` применена (`site/julcraft.db` создан). `lib/db.ts`, `drizzle.config.ts`, скрипты `db:generate`/`db:migrate` (+ `db:seed`/`db:check`), `.env`/`.env.example` (DATABASE_URL + плейсхолдеры ADMIN/TELEGRAM), `.gitignore` (node_modules, .next, .env, *.db, uploads/collages+products).

### Критерии приёмки
`npm run build` ✅, `npm run lint` ✅, хэши копий ✅, отсутствие Tailwind/импортов CSS ✅, миграции ✅.

### Уточнения / тех. адаптации (задокументированы в коде комментариями)
1. `skin-switcher.js` подключён через `next/script` c `strategy="afterInteractive"` и на `link` стоит `suppressHydrationWarning` — иначе React падает на hydration mismatch из-за инъекции скрипта (панель переключателя) и подмены `href`. Поведение макета сохранено: подменяется первый stylesheet, выбор хранится в localStorage.
2. `site/mockup/` не создан (макет остаётся в корне репо) — осознанное отступление по заданию этапа.

---

## 2. Этап 2 «Данные» (T-2.1) — ВЫПОЛНЕН

### Что сделано
- `scripts/seed.ts` + `db:seed` (идемпотентен — очистка в порядке FK и повторная вставка; **повторный запуск проверен**).
- 10 категорий (работа/срок из **публичных** страниц, D-11: Кулоны 1 000 ₽ / 3 дн, Винтажный ремонт — без шаблона слотов).
- 28 шаблонов слотов (все 9 категорий с шаблонами; min/max Кулонов из `configurator-config.html`).
- 25 комплектующих: цены/обработка/остатки/«под заказ» из components.html; 5 «под заказ» (deliveryDays 10/14/7/9/18, остаток 0); `processingDays = 0`.
- 25 SVG-заглушек — **копии inline-SVG из mockup/admin/components.html**, извлекаются сидом в `public/uploads/components/*.svg`, закоммичены в репозиторий (по заданию — часть seed-контента).
- 19 товаров (12 с витрины + 7 кулонов из category.html), описания/цены/URL фото 1:1, бейджи → поля (D-8/D-13), `reserveUntil` «Телеграммы» — ближайшая пятница.
- 9 ключей Settings (`contacts.*`, `about.short/history/principles`), telegram.* не сидится.
- `lib/get-settings.ts` — чтение из БД с фолбэком на дефолты макета.
- `scripts/check-seed.ts` (`db:check`) — счётчики и точечная сверка D-11, **все проверки OK**.

### Критерии приёмки
Счётчики 10/28/25/19/9 ✅, сверка D-11 ✅, чтение Settings из БД ✅, build/lint ✅, идемпотентность ✅.

### Уточнения (вопросы этапа 1–6 — решены предложенными дефолтами, зафиксированы в отчётах/коде)
1. **Часы**: вместо пары `hoursWeekdays/hoursWeekend` из §5 — JSON-массив из 4 строк в ключе `contacts.hours` (сохранён «Пн — выходной»). Изменение наполнения относительно §5.
2. **Тексты «О мастерской»**: `about.short/history` — JSON-чеки (строки `{label,value}` + thanks), `about.principles` — JSON-массив 4 карточек.
3. Min/max слотов (кроме Кулонов) — по предложению этапа (камень/основа/замок/шнур 1–1, подвески 0–3, бусины 0–10).
4. `processingDays` = 0 у всех (демо-цифры макета не сходятся — демонстрационные, D-11).
5. «Комплекты» — 5 слотов «как серьги + брошь» (Камень (серьги), Швензы, Камень (брошь), Подвески-дополнения, Основа броши).
6. Slug'и категорий и 7 кулонов — транслитерация по образцу админ-таблицы.

---

## 3. Этап 3 «Публичная часть» (T-3.1 … T-3.5) — ВЫПОЛНЕН

### Что сделано
- **Рефакторинг layout**: корневой layout минимальный (html + скин + скрипт), `(public)/layout.tsx` = Header+Footer; `app/not-found.tsx` — полная копия error.html (свой `ErrorHeader`: логотип с `small`, «Витрина», «⏻» → `/admin/login`, «Вход мастера →», отдельный footer).
- **Утилиты** `lib/format.ts`: `formatPrice` (ru-RU), `plural`, `reserveDayShort/Full`, `imgWith` (w=300/900).
- **Компоненты-копии макета**: ProductCard, CategoryCard, CategoryIcon (10 иконок из home.html), Crumbs, Avail (2 формы), EmptyState, SkeletonShelf, HoursBoard, CategorySort (select → URL), OrderModal, ContactForm, ProductGallery, статичные подписи `category-captions.ts`.
- **Страницы**:
  - `/` — витрина (правило R-1: feat → new → остальные, лимит 12), плитка категорий (desc — короткая статичная подпись; count — «N изделий/комплектов», ремонт — «от 300 ₽»), CTA-баннер, чек-тизер и часы из Settings;
  - `/catalog` (без демо-секции) и `/catalog/[slug]` (фильтры цена/наличие/новинки + сортировка через `?price&avail&sort&page`, «найдено: N» до пагинации, pageSize 9, пагинация только при >1 страницы, EmptyState при 0 результатов, `loading.tsx` со скелетоном, несуществующий slug → `notFound()`);
  - `/product/[slug]` (галерея + SVG-слоты до 4, чипы и характеристики только если не пусто, тег наличия, модалка заявки → POST `/api/orders` → редирект, «Собрать похожий →» по D-9, «Рядом на полке» до 3);
  - `/configurator` (9 категорий с `hasSlotTemplate` + карточка «Винтажный ремонт» `is-disabled` без ссылки; «работа от X ₽ · Y дн» из БД; notice с формулами);
  - `/about`, `/contacts` (форма-чек → POST `/api/contact`, карта-плейсхолдер 1:1, борд «Связаться напрямую» из Settings), `/order-success/[id]` (чек по типу заявки), `/404`.
- **API**: `POST /api/orders` (Zod; только `type=product`; серверный расчёт `calcPrice=price`, `calcDays=0`, `status=new`; уведомление — `console.log`), `POST /api/contact` (Zod, `channel: phone|telegram|email`, Order type=contact, D-14).
- **R-2**: миграция `0001_strong_pride.sql` (products.materials/specs, default `'[]'` — старые строки не ломает), seed доработан («Телеграмма» — чипы и характеристики дословно из product.html, остальные — `[]`), повторный сид идемпотентен.
- **R-3**: чек `/order-success/[id]` по типу (product — без СРОК/КОЛЛАЖ; custom — слой готов на Этап 5; contact — СООБЩЕНИЕ); /contacts редиректит на чек.
- Метаданные-титулы всех страниц из макета; `next/image` не используется (обычные `<img>` c lazy/decoding, w=800/900/300).

### Критерии приёмки
Все 9 маршрутов реализованы ✅, демо-секции не рендерятся ✅, данные из БД ✅, фильтры/пагинация через URL ✅, заявки пишутся в `orders` ✅ (проверено логикой route + сборкой; живые POST в ревью не выполнялись), чипы/характеристики Телеграммы дословно ✅, build/lint ✅.

### Уточнения (вопросы этапа 1–5 — решены предложенными дефолтами)
1. Подписи категорий (плитка главной, конфигуратор) — статичные строки 1:1 из макета по slug (`category-captions.ts`), не производные от БД.
2. Галерея: N фото (до 4) + добивка SVG-эскизами макета (эллипс `#e8b64c`, ромб `#d0785a`).
3. Пагинация — только при >1 странице.
4. `out_of_stock` на полке — «✕ нет на складе».
5. Статусы в чеке: «новая · в очереди к верстаку» / «в работе» / «готова» / «отменена».

---

## 4. НЕ ВЫПОЛНЕНО / замечания

| № | Замечание | Важность |
|---|---|---|
| 1 | **Чекбоксы T-1.1…T-3.5 в `plan-2.md` §8 не отмечены** — в файле нет ни одного `- [x]`, хотя задания этапов требуют отметки после приёмки | высокая (процесс) |
| 2 | **Журнал изменений `plan-2.md` не пополнен** записью о R-2 (products.materials/specs) — запись требовалась отчётом Этапа 3 | средняя (процесс) |
| 3 | `npm run build` выводит warning Next 16 о статическом доступе к файлу БД в `lib/db.ts` (server tracing всех файлов) — не блокирует, учесть на Этапе 7 (деплой/размер бандла) или добавить `/* turbopackIgnore */` | низкая |
| 4 | Визуальная приёмка «страница = макет» (браузерная сверка, работа переключателя скинов вживую) в этом ревью не проводилась — проверены build/lint/seed/check и код; финальный визуальный дифф — на усмотрение руководителя | низкая |
| 5 | Footer: соц-иконки `href="#"` — это **точная копия** макета home.html (не дефект); на /contacts ссылки соцсетей берутся из Settings | — |

### Что не входило в этапы 1–3 (корректно отложено)
Конфигуратор-конфиг `/configurator/[slug]` (Этап 5), админка и авторизация (Этап 4), уведомления Telegram (заглушка `console.log`), next/image/SEO (Этап 6), Яндекс-карта embed (плейсхолдер), uploads.

---

## 5. Рекомендации

1. Отметить `[x]` T-1.1…T-1.3, T-2.1, T-3.1…T-3.5 в `plan-2.md` §8 и добавить в «Журнал изменений» запись:
   `2026-08-15 | Этап 3: добавлены поля products.materials/specs (чипы и характеристики карточки товара) по решению руководителя (R-2)`
2. Перед Этапом 4/5: решить судьбу предупреждения `lib/db.ts` (turbopackIgnore) — не блокер.
3. Провести браузерную сверку ключевых страниц с макетом (этап 3, критерий 1) — в ревью подтверждены структура/классы/тексты по коду, но не пиксель-диф.
