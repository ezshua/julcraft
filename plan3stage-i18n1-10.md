# Задание агенту — i18n-1 · Шаг 10 из 12: Чек заявки, error.tsx, not-found

**Тип задачи:** вынос строк в словарь
**Зависимости:** i18n-1 · Шаг 1
**Связанные пункты плана:** `plan-3-i18n-constants.md` §1.8
**Файлы зоны:** `app/(public)/order-success/[id]/page.tsx`, `app/(public)/error.tsx`, `app/not-found.tsx`

## Цель

UI-строки финала воронки и служебных страниц — в словарь (namespaces `orderSuccess`, `errors`). SEO-заголовки (title этих страниц) — шаг 11.

## Шаги

1. Ключи `orderSuccess.*` (сверять с кодом):
   - статусы заявок «новая · в очереди к верстаку», «в работе», «готова», «отменена», «на столе, среди чертежей» (17–24) — выносятся вместе с `lib/order-status-labels.ts` (см. шаг 12);
   - строки чека «ТИП/СОСТАВ/ЦЕНА/СРОК/КОНТАКТ/СООБЩЕНИЕ/СТАТУС/ОТ», «товар», «конфигуратор · {категория}», «день/дня/дней» (43–88) — plural;
   - «✹ записка получена / чек получен ✹», «Подтверждаю!»/«Заявка принята!», «Юля уже заметила конверт…»/«…ставит чайник» (119–122, 129), alt-тексты (141, 151), «*** ЧТО ДАЛЬШЕ ***» + 2 абзаца (156–166), «На главную», «Смотреть каталог» (171–174). Название категории в «конфигуратор · {категория}» — из БД (i18n-2), пока интерполяция текущего значения.
2. Ключи `errors.*`:
   - error.tsx (27–38): «Что-то сломалось», «Брошь выскользнула из рук…», «Попробуйте ещё раз или вернитесь на витрину.», «На главную», «Попробовать снова»;
   - not-found.tsx (6–63): title, «Ой, 404», «Такой страницы нет — брошь здесь не висела.», «Проверьте, не потерялась ли она в каталоге.», «На главную», «Смотреть каталог», «© {year} JulCraft · с 2002 года (почти)», aria-label «Контакты». Адрес «ул. Мстислава Скрипника, 40А — на пятачке» — из Settings/`lib/settings.ts` (не трогать, i18n-2).
3. EN/UK — черновой перевод (тон «тёплой мастерской» сохранить), список — в отчёт.

## Критерии приёмки

1. RU: чек заявки для обоих типов (товар и конфигуратор — создать заявки на сид-данных), error-страница и /404 — текстово идентичны.
2. EN/UK переключаются, включая оба варианта чека (записка/чек).
3. Grep зоны — 0 кириллицы вне комментариев; `check:i18n`; `npm run lint`, `npm run build`.

## Не делать (out of scope)

- SEO-titles (шаг 11), статусные лейблы из lib (шаг 12), Settings-контент (i18n-2).
