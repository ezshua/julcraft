# Задание агенту — i18n-2 · Шаг 5 из 8: Вывод через L() — комплектующие и типы

**Тип задачи:** замена вывода
**Зависимости:** i18n-2 · Шаги 1–2
**Связанные пункты плана:** `plan-3.md` §4
**Файлы зоны:** `components/configurator/ConfiguratorClient.tsx` (названия комплектующих в слотах), витрина конфигуратора, чек заявки (состав конфигуратора), `lib/component-types.ts` (фолбэк-подписи типов), админка — НЕТ (i18n-4)

## Цель

Названия комплектующих и типов — через `L(value, locale)` в витрине (конфигуратор, чек заявки). RU не меняется.

## Шаги

1. Вывод `component.name` в конфигураторе (слоты, «N шт на складе», список выбранных), чек заявки («состав: X»),_collage-заголовки.
2. `lib/component-types.ts`: фолбэк-подписи («Камень/Подвеска/…») → per-locale словарь (используются и в витрине — конфигуратор; админские места до i18n-4 продолжают RU).
3. RU-сверка: конфигуратор с выбранными компонентами, чек заявки-конфигуратора.

## Критерии приёмки

1. RU: тексты те же (конфигуратор, чек).
2. EN/UK: тестовый компонент с en-значением отображается; фолбэк ru работает.
3. Grep зоны; `npm run lint`, `npm run build`.

## Не делать (out of scope)

- Settings (шаг 6), админка (i18n-4), переводы самих данных (i18n-5).
