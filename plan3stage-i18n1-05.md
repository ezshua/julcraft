# Задание агенту — i18n-1 · Шаг 5 из 12: Главная страница

**Тип задачи:** вынос строк в словарь
**Зависимости:** i18n-1 · Шаг 1 (ядро); желательно 3 (проверка переключением)
**Связанные пункты плана:** `plan-3-i18n-constants.md` §1.2
**Файлы зоны:** `app/(public)/page.tsx` (+ задействованные компоненты главной)

## Цель

UI-строки главной — в словарь (namespace `home`). SEO (строки 16–23) НЕ трогать — шаг 11.

## Шаги

1. Ключи `home.*` по аудиту (строки могли дрейфовать — сверять с кодом):
   - plural-статусы вывески-героя (60–62): «комплект/комплекта/комплектов», «изделие/изделия/изделий» — формы в словаре, вывод через `plural()`; «от {price} ₴» — интерполяция;
   - вывеска-герой (68–86): «✹ эст. 2026 · открыто снова ✹», тэглайн, «Смотреть каталог», «Собрать своё», «Сегодня на витрине», «// всё в одном экземпляре …»;
   - «Разложено по полкам» + подпись (101–102);
   - CTA-блок (125–133): «Соберите своё украшение», описание, «Открыть конфигуратор →»;
   - «◍ ЧЕК ЗНАКОМСТВА ◍» (141) — только заголовок, содержимое из Settings (i18n-2);
   - блок «Мастерская работает» (156–167) — все абзацы.
2. Заменить хардкод на `t()`; интерполяции (`{price}`, `{n}`) через параметры.
3. EN/UK — черновой перевод (тон мастерской сохранить), список — в отчёт.

## Критерии приёмки

1. RU-главная текстово и визуально идентична (сверка всех блоков).
2. EN/UK переключаются; формы plural корректны на 1/2/5/21.
3. Grep: в `page.tsx` нет кириллицы вне комментариев (кроме импортов SEO-строк до шага 11).
4. `check:i18n`, `npm run lint`, `npm run build`.

## Не делать (out of scope)

- Metadata главной (шаг 11), контент Settings (i18n-2), логика plural-по-slug не меняется — только формы из словаря.
